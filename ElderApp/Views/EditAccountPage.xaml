<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="ElderApp.Views.EditAccountPage"
             BackgroundColor="#ffffff">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="24">
       
            
                <Label HeightRequest="40" TextColor="#919191" Text="姓名"></Label>
                <Entry HeightRequest="60"  Text="{Binding Name,Mode=TwoWay}" Margin="0,0,0,24"></Entry>
            
            
                <Label HeightRequest="40" TextColor="#919191" Text="手機"></Label>
                <Entry HeightRequest="60" Text="{Binding Phone,Mode=TwoWay}" Margin="0,0,0,24"></Entry>
            
            
                <Label HeightRequest="40" TextColor="#919191" Text="住家電話"></Label>
                <Entry HeightRequest="60" Text="{Binding Tel,Mode=TwoWay}" Margin="0,0,0,24"></Entry>
            
            
                <Label HeightRequest="40" TextColor="#919191" Text="地址"></Label>
                <Entry HeightRequest="60" Text="{Binding Address,Mode=TwoWay}" Margin="0,0,0,24"></Entry>
            
            
                <Label HeightRequest="40" TextColor="#919191" Text="身分證號"></Label>
                <Entry HeightRequest="60" Text="{Binding Id_number,Mode=TwoWay}" Margin="0,0,0,24"></Entry>
            
            
            
                <Button Text="確定修改" BackgroundColor="#FEA735" TextColor="White" Margin="0,0,0,12" HeightRequest="60" Command="{Binding Submit}"/>
                <Button Text="取消" BackgroundColor="Gray" TextColor="White" Margin="0,0,0,12" HeightRequest="60" Command="{Binding Cancel}"/>
            
            </StackLayout>
        </ScrollView>
        
    </ContentPage.Content>
</ContentPage>
